# Erlang version
erlang_version=18.1

# Elixir version
elixir_version=1.1.1

# Env vars
config_vars_to_export=(REDIS_HOST REDIS_HOST REDIS_PORT)

# Always rebuild from scratch on every deploy?
always_rebuild=false
